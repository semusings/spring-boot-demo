server:
  port: 9090
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        use_sql_comments: true
        format_sql: true
  security:
    user:
      name: semusings
      password: semusings
openfga:
  api-url: http://localhost:4000
  store-id: inventory
  authorization-model-id: inventory
